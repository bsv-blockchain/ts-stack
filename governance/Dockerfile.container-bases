# Dependency-discovery metadata only; this file is never built or released.
# Dependabot updates this canonical Docker Hub tag. Repository health then
# requires every governed registry reference and release Dockerfile to be
# reconciled to the verified version and digest in the same PR.
FROM node:24.20.0-alpine3.24@sha256:e67514e5d0f6c46656005e1b693b2ec9d52e80b641307de684d4a015ba7a4eaf AS dockerhub-node-base
USER node
