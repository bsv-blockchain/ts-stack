export default `
# SupplyChain Topic Manager Documentation

The **SupplyChain Topic Manager** (topic ID: \`tm_supplychain\`) lets clients upload Pushdrop transactions.

Pushdrop transactions with off-chain values are indexed by the **SupplyChain Lookup Service**.
`
