export default `# Classic STAS Token Lookup Service (ls_stas)

Indexes admitted classic-STAS token UTXOs by assetId (symbol), owner hash160,
and outpoint. Supports lookup by assetId, by owner, or by outpoint. There is no
key-linkage retention and no identity-balance query — classic STAS carries no
identity linkage on-chain.
`
