export default `# DSTAS Token Lookup Service (ls_dstas)

Indexes admitted DSTAS token UTXOs by tokenId (redemption pkh), owner hash160,
and outpoint, and records the frozen marker. Supports lookup by tokenId, by
owner, or by outpoint. This is the primary public index for DSTAS, which has no
third-party indexer.
`
