export default `# BSV-21 Token Lookup Service (ls_bsv21)

Indexes admitted BSV-21 token UTXOs by tokenId, owner hash160, and outpoint.
Supports lookup by tokenId, by owner, or by outpoint. Retains the raw bigint
amount (as a string) for explorer use. No identity linkage.
`
