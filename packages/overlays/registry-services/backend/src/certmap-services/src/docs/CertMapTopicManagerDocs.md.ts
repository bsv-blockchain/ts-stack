export default `# CertMap Topic Manager Documentation

      The CertMap Topic Manager is responsible for managing the rules of admissibility for CertMap tokens and handling transactions related to them.

      ## Admissibility Rules

      - The transaction must have valid inputs and outputs.
      - Each output must be decoded and validated according to the CertMap protocol.
      - The type, name, icon URL, description, and documentation URL must be valid and match the expected formats.
      - The registry operator must be correctly identified and verified.
      - The signature must be verified to ensure it is valid.
      \`\`\``
