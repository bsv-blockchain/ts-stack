[project]
name = "ts-stack-codegen"
version = "0.0.0"
requires-python = "==3.12.*"
dependencies = [
  "datamodel-code-generator==0.71.0",
]

[tool.uv]
package = false
# CI pins uv exactly; allow the compatible resolver provided by Dependabot.
required-version = ">=0.11.32"
exclude-newer = "1 week"
