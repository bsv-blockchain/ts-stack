apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -o deploy/
    kompose.version: 1.34.0 (cbf2835db)
  labels:
    io.kompose.service: app
  name: app
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    io.kompose.service: app
